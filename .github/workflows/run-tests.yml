name: Run Playwright Tests 🎭

on:
  push:
    branches: [ main ]

concurrency: production_environment

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout ⚙️
        uses: actions/checkout@v4

      - name: Set up Node.js and install dependencies ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies and get Playwright version 📦
        run: |
          npm ci
          PLAYWRIGHT_VERSION=$(npm ls --json @playwright/test | jq --raw-output '.dependencies["@playwright/test"].version')
          echo "PLAYWRIGHT_VERSION=$PLAYWRIGHT_VERSION" >> $GITHUB_ENV

      - name: 🧰 Cache Playwright browser binaries
        uses: actions/cache@v3
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ env.PLAYWRIGHT_VERSION }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright browsers & deps
        run: |
          if [[ "${{ steps.playwright-cache.outputs.cache-hit }}" == "true" ]]; then
            npx playwright install-deps
          else
            npx playwright install --with-deps
          fi

      - name: Start Docker containers 🐳
        run: |
          export DISABLE_LOGGING=true
          docker compose -f docker-compose.yml up -d

      - name: Wait for Laravel API to become ready ⏳
        run: |
          echo "Waiting for Laravel to be ready..."
          for i in {1..30}; do
            if curl -sf http://localhost:8091/status > /dev/null; then
              echo "Laravel is up!"
              break
            fi
            echo "Still waiting... ($i)"
            sleep 2
          done
          curl -X GET http://localhost:8091/status

      - name: Create & Seed database 🌱
        run: |
          docker compose exec -T laravel-api php artisan migrate:refresh --seed

      - name: POST login (verify auth flow)
        run: |
          curl -v -X POST 'http://localhost:8091/users/login' \
          -H 'Content-Type: application/json' \
          --data-raw '{"email":"customer@practicesoftwaretesting.com","password":"welcome01"}'

      - name: Run Playwright tests 🧪
        run: npx playwright test

      - name: Upload Playwright Report 📊
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 10
